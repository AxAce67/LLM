name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  python-syntax:
    name: Python Syntax Check
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Compile Python files
        run: |
          set -euo pipefail
          python -V
          mapfile -t files < <(find . -type f -name '*.py' -not -path './venv/*' -not -path './.venv/*')
          echo "python files: ${#files[@]}"
          if [ "${#files[@]}" -eq 0 ]; then
            echo "no python files found"
            exit 1
          fi
          python -m py_compile "${files[@]}"

  unit-tests:
    name: Unit Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install test dependencies
        run: |
          set -euo pipefail
          python -m pip install --upgrade pip
          pip install \
            pytest==8.3.3 \
            sentencepiece==0.2.0 \
            torch==2.2.2 \
            requests==2.32.3 \
            beautifulsoup4==4.12.3 \
            psycopg2-binary==2.9.9 \
            python-dotenv==1.0.1 \
            psutil==6.0.0

      - name: Run legacy tests
        run: |
          set -euo pipefail
          PYTHONPATH=legacy pytest -q legacy/tests

      - name: Run core_llm tests
        run: |
          set -euo pipefail
          PYTHONPATH=core_llm/src pytest -q core_llm/tests

  compose-validate:
    name: Docker Compose Validate
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate docker compose
        run: |
          set -euo pipefail
          docker compose version
          cp legacy/.env.example legacy/.env
          docker compose --env-file legacy/.env -f legacy/docker-compose.yml config >/tmp/compose.config.out
          test -s /tmp/compose.config.out
          echo "legacy/docker-compose.yml is valid"

  repo-guards:
    name: Repo Guards
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Prevent accidental secret file commits
        run: |
          set -euo pipefail
          banned_files=(
            ".env"
            ".env.local"
            ".env.production"
            ".env.development"
          )
          for f in "${banned_files[@]}"; do
            if git ls-files --error-unmatch "$f" >/dev/null 2>&1; then
              echo "error: sensitive file is tracked: $f"
              exit 1
            fi
          done
          echo "secret file guard passed"

      - name: Verify env template exists
        run: |
          set -euo pipefail
          test -f legacy/.env.example
          grep -q "SYSTEM_ROLE" legacy/.env.example
          echo "legacy/.env.example sanity check passed"
