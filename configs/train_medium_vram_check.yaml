# 104Mモデルのメモリ確認用（小データで動作確認する）
batch_size: 2
seq_len: 512
learning_rate: 1e-4
weight_decay: 0.1
grad_accum_steps: 4
warmup_steps: 50
total_steps: 200
eval_every: 50
save_every: 100
seed: 42
device: auto
amp: true
min_lr_ratio: 0.1
grad_clip: 1.0
early_stopping_patience: 10
